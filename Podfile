# Uncomment the next line to define a global platform for your project
platform :ios, '14.0'

target 'CoronaVirTracker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CoronaVirTracker
  # pod 'Mapbox-iOS-SDK'
  pod 'SwiftKeychainWrapper'
  # pod 'MapboxGeocoder.swift' Used in previous versions of the app
  # pod 'Charts' #, '3.6.0'
  pod 'PhoneNumberKit'
  pod 'Firebase/Auth'
  pod 'RealmSwift'
end

target 'WatchVPN' do
  use_frameworks!
  pod 'SwiftKeychainWrapper'
end
